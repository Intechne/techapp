# Public values only. The admin panel uses the same publishable key as the mobile app: every permission is enforced
# by RLS and RPCs on the server, never by this client. NEVER put a secret / service_role key here.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
